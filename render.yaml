services:
  - type: web
    name: smartexpense-backend
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn smartexpense.wsgi:application"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: smartexpense-db
          property: connectionString
